# ── Training Environment — Python 3.12 + CUDA 12.4 (RTX 3050) ────────────────
# PyTorch CUDA builds require Python ≤ 3.12.
# Run setup_training_env.ps1 to automate the full setup, OR manually:
#
#   C:\Python312\python.exe -m venv venv312
#   .\venv312\Scripts\Activate.ps1
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-train.txt
# ─────────────────────────────────────────────────────────────────────────────
# NOTE: install torch separately via the cu124 index (see above).
# The line below is for CPU fallback only — remove it if you used the CUDA index.
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter
jupyterlab>=4.0.0
ipywidgets>=8.0.0
ipykernel>=6.0.0
