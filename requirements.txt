# ── FastAPI Web Server — works on Python 3.14+ ───────────────────────────────
# Install with:  pip install -r requirements.txt
# Run server  :  python -m uvicorn app:app --host 0.0.0.0 --port 8000
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.0.0
python-multipart>=0.0.9
jinja2>=3.1.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
torch>=2.0.0
